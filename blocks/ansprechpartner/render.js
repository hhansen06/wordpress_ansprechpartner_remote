/**
 * Frontend rendering script
 * This file is enqueued on the frontend for any dynamic functionality
 */

document.addEventListener('DOMContentLoaded', function() {
	// Optional: Add any frontend interactivity here
	// For example, modal functionality, lazy loading, etc.
	
	const cards = document.querySelectorAll('.war-card');
	cards.forEach(card => {
		// Add any frontend enhancements
	});
});
